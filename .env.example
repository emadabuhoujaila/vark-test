# Server
PORT=3001
NODE_ENV=development

# Teacher login (default if not set in DB)
TEACHER_PIN=teacher2024

# Optional: PostgreSQL (Neon, Supabase, Railway, etc.)
# Leave empty to use local SQLite (data/vark.db)
# DATABASE_URL=postgresql://user:password@host:5432/vark_test
# PGSSL=true

# Frontend (production only — leave empty in dev; Vite proxy handles /api)
# VITE_API_URL=
